package fortal.examples.lexer;

/*
    this is a multiline comment

    also known as a
    block comment
*/

import fortal.test.import_test;

/* multiline */

//  this is a single line comment
//      this is a single line comment
//          this is a single line comment

void test(int32 a, int32 b)
{
    a += b;
}

int32 main()
{
    int32 y;
    if (y != y) return y;
    int32 x = 0 + 0;
    if (x + 2 == 3) return 1;
    else return 0;

    test(x, y);

    -1.000003
        3
        - 1
        0
        5.5
        5.163813681435419142
        1e-2
        1e20
        "test"
        't'
        'cttttt'
        x &= (false | true)
             return null;
}
int32