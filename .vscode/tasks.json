{
    "tasks": [
        {
            "label": "build",
            "command": "make",
            "args": ["-j"],
            "options": {
                "cwd": "${workspaceFolder}/build/Debug"
            }
        }
    ],
    "version": "2.0.0"
}
